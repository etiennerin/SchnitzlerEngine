Adding elements to the code :

Components :

For components that need to be rendered the object"mech" should always be named "this._object"
This component should also have à "this._addScene" attribute set to false initially
